<div class="space-y-6">
  <div class="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
    <div>
      <p class="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Create</p>
      <h1 class="text-2xl font-semibold text-foreground">New <%= @model.name %></h1>
    </div>
    <%= render "ui/navigation_menu/link", href: darwin.records_path(@model.name.pluralize.underscore), trigger_style: true do %>
      Back
    <% end %>
  </div>

  <%= render 'form' %>
</div>
