<div class="mx-auto max-w-3xl space-y-6">
  <header class="space-y-1">
    <p class="text-xs font-semibold uppercase tracking-wide text-slate-500">Model</p>
    <h1 class="text-2xl font-semibold text-slate-900"><%= @model.name %></h1>
    <p class="text-sm text-slate-600">Edit blocks for this model. Changes stream in via Turbo.</p>
  </header>

  <%= render "darwin/v2/models/block_list", model: @model %>
</div>
