<!DOCTYPE html>
<html>
  <head>
    <title>Darwin Engine</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "darwin/tailwind", "data-turbo-track": "reload" %>
    <%= darwin_editor_javascript_tags %>
  </head>
  <body class="darwin-shell">
    <header class="darwin-header">
      <div class="darwin-header__inner">
        <div>
          <div class="darwin-header__title">Darwin Studio</div>
          <div class="darwin-header__subtitle">Runtime models, blocks & records</div>
        </div>
        <div class="darwin-actions">
          <%= link_to "Models", darwin.root_path, class: "darwin-btn darwin-btn--ghost" %>
          <%= link_to "New Model", darwin.new_model_path, class: "darwin-btn darwin-btn--primary" %>
        </div>
      </div>
    </header>

    <main class="darwin-page">
      <%= yield %>
    </main>
  </body>
</html>
